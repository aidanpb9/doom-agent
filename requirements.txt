# DoomSat Dependencies
# Install with: python3.10 -m pip install -r requirements.txt
# Note: vizdoom requires Python 3.10. Use python3.10 explicitly on systems with multiple versions.

# Core game engine
vizdoom>=1.1.13

# System monitoring (optional, used for rss_mb in Tier 0 telemetry)
psutil>=5.9.0

# Post-run analysis
matplotlib>=3.7.0

# Testing
pytest>=7.0.0
