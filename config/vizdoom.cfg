# VizDoom Configuration File

# Screen settings
screen_resolution = RES_640X480
screen_format = RGB24
window_visible = true

# Rendering options
render_hud = true
render_crosshair = true
render_weapon = true
render_decals = true
render_particles = true
render_messages = true
render_corpses = true

# Buffers for perception
labels_buffer_enabled = true
depth_buffer_enabled = false 
automap_buffer_enabled = false
automap_rotate = true
automap_mode = WHOLE
automap_render_textures = false

# Game variables
available_game_variables =
{
    HEALTH
    ARMOR
    AMMO2
    POSITION_X
    POSITION_Y
    ANGLE
    KILLCOUNT
}

# Buttons
available_buttons =
{
    MOVE_FORWARD
    MOVE_BACKWARD
    TURN_LEFT
    TURN_RIGHT
    ATTACK
    USE
}

# Episode settings
episode_start_time = 10
doom_skill = 3
